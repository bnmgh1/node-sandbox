globalMy.MutationRecord_get_type = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_type');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['type'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_type, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_target = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_target');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['target'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_target, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_addedNodes = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_addedNodes');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['addedNodes'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_addedNodes, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_removedNodes = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_removedNodes');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['removedNodes'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_removedNodes, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_previousSibling = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_previousSibling');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['previousSibling'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_previousSibling, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_nextSibling = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_nextSibling');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['nextSibling'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_nextSibling, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_attributeName = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_attributeName');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['attributeName'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_attributeName, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_attributeNamespace = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_attributeNamespace');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['attributeNamespace'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_attributeNamespace, result => ', '' + result);
  }
  return result;
};
globalMy.MutationRecord_get_oldValue = function () {
  if (!(this instanceof MutationRecord)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_oldValue');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['oldValue'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了MutationRecord_get_oldValue, result => ', '' + result);
  }
  return result;
};
// Navigator
// Navigator