globalMy.XMLHttpRequestEventTarget_get_onloadstart = function () {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['onloadstart'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_get_onloadstart,result => ', '' + result);
  }
  return result;
};
globalMy.XMLHttpRequestEventTarget_set_onloadstart = function (val) {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  globalMy.value[foundName]['onloadstart'] = val;
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_set_onloadstart, 传参val => ' + val + '  result => ', '' + result);
  }
};
globalMy.XMLHttpRequestEventTarget_get_onprogress = function () {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['onprogress'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_get_onprogress,result => ', '' + result);
  }
  return result;
};
globalMy.XMLHttpRequestEventTarget_set_onprogress = function (val) {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  globalMy.value[foundName]['onprogress'] = val;
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_set_onprogress, 传参val => ' + val + '  result => ', '' + result);
  }
};
globalMy.XMLHttpRequestEventTarget_get_onabort = function () {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['onabort'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_get_onabort,result => ', '' + result);
  }
  return result;
};
globalMy.XMLHttpRequestEventTarget_set_onabort = function (val) {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  globalMy.value[foundName]['onabort'] = val;
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_set_onabort, 传参val => ' + val + '  result => ', '' + result);
  }
};
globalMy.XMLHttpRequestEventTarget_get_onerror = function () {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['onerror'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_get_onerror,result => ', '' + result);
  }
  return result;
};
globalMy.XMLHttpRequestEventTarget_set_onerror = function (val) {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  globalMy.value[foundName]['onerror'] = val;
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_set_onerror, 传参val => ' + val + '  result => ', '' + result);
  }
};
globalMy.XMLHttpRequestEventTarget_get_onload = function () {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['onload'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_get_onload,result => ', '' + result);
  }
  return result;
};
globalMy.XMLHttpRequestEventTarget_set_onload = function (val) {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  globalMy.value[foundName]['onload'] = val;
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_set_onload, 传参val => ' + val + '  result => ', '' + result);
  }
};
globalMy.XMLHttpRequestEventTarget_get_ontimeout = function () {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['ontimeout'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_get_ontimeout,result => ', '' + result);
  }
  return result;
};
globalMy.XMLHttpRequestEventTarget_set_ontimeout = function (val) {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  globalMy.value[foundName]['ontimeout'] = val;
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_set_ontimeout, 传参val => ' + val + '  result => ', '' + result);
  }
};
globalMy.XMLHttpRequestEventTarget_get_onloadend = function () {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.value[foundName]['onloadend'];
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_get_onloadend,result => ', '' + result);
  }
  return result;
};
globalMy.XMLHttpRequestEventTarget_set_onloadend = function (val) {
  if (!(this instanceof XMLHttpRequestEventTarget)) {
    throw new TypeError("Illegal invocation");
  }
  var result;
  var foundName = globalMy.foundName(this);
  globalMy.value[foundName]['onloadend'] = val;
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了XMLHttpRequestEventTarget_set_onloadend, 传参val => ' + val + '  result => ', '' + result);
  }
};
// dom构造函数原型方法
// dom构造函数原型方法