globalMy.WebGL2RenderingContext_get_canvas = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    var foundName = globalMy.foundName(this);
    result = globalMy.value[foundName]['canvas'];
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_get_canvas,result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_get_drawingBufferWidth = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    var foundName = globalMy.foundName(this);
    result = globalMy.value[foundName]['drawingBufferWidth'];
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_get_drawingBufferWidth,result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_get_drawingBufferHeight = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    var foundName = globalMy.foundName(this);
    result = globalMy.value[foundName]['drawingBufferHeight'];
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_get_drawingBufferHeight,result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_get_drawingBufferColorSpace = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    var foundName = globalMy.foundName(this);
    result = globalMy.value[foundName]['drawingBufferColorSpace'];
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_get_drawingBufferColorSpace,result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_set_drawingBufferColorSpace = function (val) {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    var foundName = globalMy.foundName(this);
    globalMy.value[foundName]['drawingBufferColorSpace'] = val;
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_set_drawingBufferColorSpace, 传参val => ' + val + '  result => ', result)
    }
}
globalMy.WebGL2RenderingContext_get_unpackColorSpace = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    var foundName = globalMy.foundName(this);
    result = globalMy.value[foundName]['unpackColorSpace'];
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_get_unpackColorSpace,result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_set_unpackColorSpace = function (val) {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    var foundName = globalMy.foundName(this);
    globalMy.value[foundName]['unpackColorSpace'] = val;
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_set_unpackColorSpace, 传参val => ' + val + '  result => ', result)
    }
}
globalMy.WebGL2RenderingContext_activeTexture = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_activeTexture, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_attachShader = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_attachShader, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_beginQuery = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_beginQuery, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_beginTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_beginTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindAttribLocation = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindAttribLocation, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindBufferBase = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindBufferBase, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindBufferRange = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindBufferRange, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindRenderbuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindRenderbuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindSampler = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindSampler, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindVertexArray = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindVertexArray, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_blendColor = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_blendColor, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_blendEquation = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_blendEquation, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_blendEquationSeparate = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_blendEquationSeparate, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_blendFunc = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_blendFunc, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_blendFuncSeparate = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_blendFuncSeparate, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_blitFramebuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_blitFramebuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bufferData = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bufferData, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bufferSubData = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bufferSubData, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_checkFramebufferStatus = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_checkFramebufferStatus, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clientWaitSync = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clientWaitSync, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_compileShader = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_compileShader, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_compressedTexImage2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_compressedTexImage2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_compressedTexImage3D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_compressedTexImage3D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_compressedTexSubImage2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_compressedTexSubImage2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_compressedTexSubImage3D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_compressedTexSubImage3D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_copyBufferSubData = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_copyBufferSubData, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_copyTexImage2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_copyTexImage2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_copyTexSubImage2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_copyTexSubImage2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_copyTexSubImage3D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_copyTexSubImage3D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createBuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createBuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createFramebuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createFramebuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createProgram = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createProgram, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createQuery = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createQuery, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createRenderbuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createRenderbuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createSampler = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createSampler, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createShader = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createShader, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createTexture = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createTexture, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_createVertexArray = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_createVertexArray, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_cullFace = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_cullFace, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteBuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteBuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteFramebuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteFramebuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteProgram = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteProgram, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteQuery = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteQuery, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteRenderbuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteRenderbuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteSampler = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteSampler, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteShader = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteShader, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteSync = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteSync, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteTexture = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteTexture, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_deleteVertexArray = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_deleteVertexArray, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_depthFunc = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_depthFunc, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_depthMask = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_depthMask, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_depthRange = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_depthRange, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_detachShader = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_detachShader, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_disable = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_disable, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_drawArraysInstanced = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_drawArraysInstanced, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_drawElementsInstanced = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_drawElementsInstanced, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_drawRangeElements = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_drawRangeElements, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_enable = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_enable, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_endQuery = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_endQuery, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_endTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_endTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_fenceSync = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_fenceSync, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_finish = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_finish, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_flush = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_flush, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_framebufferRenderbuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_framebufferRenderbuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_framebufferTexture2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_framebufferTexture2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_framebufferTextureLayer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_framebufferTextureLayer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_frontFace = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_frontFace, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_generateMipmap = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_generateMipmap, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getActiveAttrib = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getActiveAttrib, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getActiveUniform = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getActiveUniform, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getActiveUniformBlockName = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getActiveUniformBlockName, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getActiveUniformBlockParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getActiveUniformBlockParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getActiveUniforms = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getActiveUniforms, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getAttachedShaders = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getAttachedShaders, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getAttribLocation = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getAttribLocation, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getBufferParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getBufferParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getBufferSubData = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getBufferSubData, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getContextAttributes = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getContextAttributes, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getError = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getError, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getExtension = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getExtension, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getFragDataLocation = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getFragDataLocation, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getFramebufferAttachmentParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getFramebufferAttachmentParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getIndexedParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getIndexedParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getInternalformatParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getInternalformatParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getProgramInfoLog = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getProgramInfoLog, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getProgramParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getProgramParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getQuery = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getQuery, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getQueryParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getQueryParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getRenderbufferParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getRenderbufferParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getSamplerParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getSamplerParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getShaderInfoLog = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getShaderInfoLog, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getShaderParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getShaderParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getShaderPrecisionFormat = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getShaderPrecisionFormat, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getShaderSource = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getShaderSource, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getSupportedExtensions = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getSupportedExtensions, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getSyncParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getSyncParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getTexParameter = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getTexParameter, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getTransformFeedbackVarying = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getTransformFeedbackVarying, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getUniform = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getUniform, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getUniformBlockIndex = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getUniformBlockIndex, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getUniformIndices = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getUniformIndices, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getUniformLocation = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getUniformLocation, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getVertexAttrib = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getVertexAttrib, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_getVertexAttribOffset = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_getVertexAttribOffset, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_hint = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_hint, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_invalidateFramebuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_invalidateFramebuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_invalidateSubFramebuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_invalidateSubFramebuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isBuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isBuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isContextLost = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isContextLost, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isEnabled = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isEnabled, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isFramebuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isFramebuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isProgram = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isProgram, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isQuery = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isQuery, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isRenderbuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isRenderbuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isSampler = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isSampler, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isShader = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isShader, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isSync = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isSync, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isTexture = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isTexture, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_isVertexArray = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_isVertexArray, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_lineWidth = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_lineWidth, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_linkProgram = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_linkProgram, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_pauseTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_pauseTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_pixelStorei = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_pixelStorei, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_polygonOffset = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_polygonOffset, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_readBuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_readBuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_readPixels = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_readPixels, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_renderbufferStorage = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_renderbufferStorage, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_renderbufferStorageMultisample = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_renderbufferStorageMultisample, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_resumeTransformFeedback = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_resumeTransformFeedback, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_sampleCoverage = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_sampleCoverage, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_samplerParameterf = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_samplerParameterf, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_samplerParameteri = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_samplerParameteri, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_shaderSource = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_shaderSource, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_stencilFunc = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_stencilFunc, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_stencilFuncSeparate = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_stencilFuncSeparate, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_stencilMask = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_stencilMask, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_stencilMaskSeparate = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_stencilMaskSeparate, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_stencilOp = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_stencilOp, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_stencilOpSeparate = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_stencilOpSeparate, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texImage2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texImage2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texImage3D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texImage3D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texParameterf = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texParameterf, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texParameteri = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texParameteri, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texStorage2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texStorage2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texStorage3D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texStorage3D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texSubImage2D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texSubImage2D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_texSubImage3D = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_texSubImage3D, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_transformFeedbackVaryings = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_transformFeedbackVaryings, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform1ui = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform1ui, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform2ui = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform2ui, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform3ui = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform3ui, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform4ui = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform4ui, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformBlockBinding = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformBlockBinding, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_useProgram = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_useProgram, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_validateProgram = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_validateProgram, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttribDivisor = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttribDivisor, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttribI4i = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttribI4i, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttribI4ui = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttribI4ui, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttribIPointer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttribIPointer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_waitSync = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_waitSync, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindBuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindBuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindFramebuffer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindFramebuffer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_bindTexture = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_bindTexture, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clear = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clear, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clearBufferfi = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clearBufferfi, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clearBufferfv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clearBufferfv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clearBufferiv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clearBufferiv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clearBufferuiv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clearBufferuiv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clearColor = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clearColor, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clearDepth = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clearDepth, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_clearStencil = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_clearStencil, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_colorMask = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_colorMask, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_disableVertexAttribArray = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_disableVertexAttribArray, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_drawArrays = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_drawArrays, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_drawBuffers = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_drawBuffers, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_drawElements = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_drawElements, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_enableVertexAttribArray = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_enableVertexAttribArray, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_scissor = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_scissor, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform1f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform1f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform1fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform1fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform1i = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform1i, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform1iv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform1iv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform1uiv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform1uiv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform2f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform2f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform2fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform2fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform2i = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform2i, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform2iv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform2iv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform2uiv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform2uiv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform3f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform3f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform3fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform3fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform3i = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform3i, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform3iv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform3iv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform3uiv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform3uiv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform4f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform4f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform4fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform4fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform4i = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform4i, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform4iv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform4iv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniform4uiv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniform4uiv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix2fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix2fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix2x3fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix2x3fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix2x4fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix2x4fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix3fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix3fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix3x2fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix3x2fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix3x4fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix3x4fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix4fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix4fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix4x2fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix4x2fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_uniformMatrix4x3fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_uniformMatrix4x3fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib1f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib1f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib1fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib1fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib2f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib2f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib2fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib2fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib3f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib3f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib3fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib3fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib4f = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib4f, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttrib4fv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttrib4fv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttribI4iv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttribI4iv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttribI4uiv = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttribI4uiv, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_vertexAttribPointer = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_vertexAttribPointer, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_viewport = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_viewport, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}
globalMy.WebGL2RenderingContext_makeXRCompatible = function () {
    if (!(this instanceof WebGL2RenderingContext)) {
        throw new TypeError("Illegal invocation");
    }
    let result;
    //这里写方法实体
    if (globalMy.is_log) {
        console.log('[*]  调用了WebGL2RenderingContext_makeXRCompatible, arguments => ', JSON.stringify(arguments), '  result => ', result)
    }
    return result;
}