globalMy.WebGLLoseContext_loseContext = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了WebGLLoseContext_loseContext, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};
globalMy.WebGLLoseContext_restoreContext = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了WebGLLoseContext_restoreContext, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};