globalMy.WebGLDebugShaders_getTranslatedShaderSource = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了WebGLDebugShaders_getTranslatedShaderSource, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};