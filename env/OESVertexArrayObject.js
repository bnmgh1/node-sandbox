globalMy.OESVertexArrayObject_bindVertexArrayOES = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了OESVertexArrayObject_bindVertexArrayOES, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};
globalMy.OESVertexArrayObject_createVertexArrayOES = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了OESVertexArrayObject_createVertexArrayOES, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};
globalMy.OESVertexArrayObject_deleteVertexArrayOES = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了OESVertexArrayObject_deleteVertexArrayOES, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};
globalMy.OESVertexArrayObject_isVertexArrayOES = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了OESVertexArrayObject_isVertexArrayOES, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};