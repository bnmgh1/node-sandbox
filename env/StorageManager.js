globalMy.StorageManager_estimate = function () {
  if (!(this instanceof StorageManager)) {
    throw new TypeError("Illegal invocation");
  }
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了StorageManager_estimate, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};
globalMy.StorageManager_getDirectory = function () {
  if (!(this instanceof StorageManager)) {
    throw new TypeError("Illegal invocation");
  }
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了StorageManager_getDirectory, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};
globalMy.StorageManager_persisted = function () {
  if (!(this instanceof StorageManager)) {
    throw new TypeError("Illegal invocation");
  }
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了StorageManager_persisted, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};
globalMy.StorageManager_persist = function () {
  if (!(this instanceof StorageManager)) {
    throw new TypeError("Illegal invocation");
  }
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了StorageManager_persist, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};