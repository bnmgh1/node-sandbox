globalMy.WebGLDrawBuffers_drawBuffersWEBGL = function () {
  let result;
  //这里写方法实体
  if (globalMy.is_log) {
    console.log('[*]  调用了WebGLDrawBuffers_drawBuffersWEBGL, arguments => ' + JSON.stringify(arguments) + '  result => ', result);
  }
  return result;
};