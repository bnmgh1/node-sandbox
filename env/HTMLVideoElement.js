globalMy.HTMLVideoElement_get_width = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_width');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['width'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_width, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_width = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_width, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['width'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['width'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_width' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_get_height = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_height');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['height'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_height, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_height = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_height, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['height'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['height'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_height' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_get_videoWidth = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_videoWidth');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['videoWidth'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_videoWidth, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_get_videoHeight = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_videoHeight');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['videoHeight'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_videoHeight, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_get_poster = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_poster');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['poster'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_poster, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_poster = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_poster, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['poster'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['poster'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_poster' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_get_webkitDecodedFrameCount = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitDecodedFrameCount');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['webkitDecodedFrameCount'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitDecodedFrameCount, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_get_webkitDroppedFrameCount = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitDroppedFrameCount');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['webkitDroppedFrameCount'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitDroppedFrameCount, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_get_playsInline = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_playsInline');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['playsInline'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_playsInline, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_playsInline = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_playsInline, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['playsInline'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['playsInline'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_playsInline' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_get_onenterpictureinpicture = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_onenterpictureinpicture');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['onenterpictureinpicture'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_onenterpictureinpicture, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_onenterpictureinpicture = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_onenterpictureinpicture, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['onenterpictureinpicture'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['onenterpictureinpicture'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_onenterpictureinpicture' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_get_onleavepictureinpicture = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_onleavepictureinpicture');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['onleavepictureinpicture'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_onleavepictureinpicture, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_onleavepictureinpicture = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_onleavepictureinpicture, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['onleavepictureinpicture'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['onleavepictureinpicture'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_onleavepictureinpicture' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_get_disablePictureInPicture = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_disablePictureInPicture');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['disablePictureInPicture'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_disablePictureInPicture, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_disablePictureInPicture = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_disablePictureInPicture, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['disablePictureInPicture'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['disablePictureInPicture'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_disablePictureInPicture' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_cancelVideoFrameCallback = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_cancelVideoFrameCallback, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.cancelVideoFrameCallback.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_cancelVideoFrameCallback ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_requestPictureInPicture = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_requestPictureInPicture, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.requestPictureInPicture.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_requestPictureInPicture ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_requestVideoFrameCallback = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_requestVideoFrameCallback, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.requestVideoFrameCallback.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_requestVideoFrameCallback ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_get_webkitSupportsFullscreen = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitSupportsFullscreen');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['webkitSupportsFullscreen'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitSupportsFullscreen, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_get_webkitDisplayingFullscreen = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitDisplayingFullscreen');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['webkitDisplayingFullscreen'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_webkitDisplayingFullscreen, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_get_msVideoProcessing = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_msVideoProcessing');
  }
  var result;
  var foundName = globalMy.foundName(this);
  result = globalMy.jsdom_element[foundName]['msVideoProcessing'];
  if (result != null && result != undefined) {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_get_msVideoProcessing, result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_set_msVideoProcessing = function (val) {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_msVideoProcessing, 传参val => ' + val);
  }
  var result;
  var foundName = globalMy.foundName(this);
  if (val == undefined || val == null) {
    globalMy.jsdom_element[foundName]['msVideoProcessing'] = val;
  } else {
    switch (typeof val) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        globalMy.jsdom_element[foundName]['msVideoProcessing'] = val;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_set_msVideoProcessing' + '  result => ', '' + result);
  }
};
globalMy.HTMLVideoElement_getVideoPlaybackQuality = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_getVideoPlaybackQuality, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.getVideoPlaybackQuality.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_getVideoPlaybackQuality ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_msGetVideoProcessingTypes = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_msGetVideoProcessingTypes, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.msGetVideoProcessingTypes.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_msGetVideoProcessingTypes ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_webkitEnterFullScreen = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitEnterFullScreen, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.webkitEnterFullScreen.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitEnterFullScreen ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_webkitEnterFullscreen = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitEnterFullscreen, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.webkitEnterFullscreen.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitEnterFullscreen ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_webkitExitFullScreen = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitExitFullScreen, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.webkitExitFullScreen.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitExitFullScreen ' + '  result => ', '' + result);
  }
  return result;
};
globalMy.HTMLVideoElement_webkitExitFullscreen = function () {
  if (!(this instanceof HTMLVideoElement)) {
    throw new TypeError("Illegal invocation");
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitExitFullscreen, arguments => ', arguments);
  }
  var result;
  //这里写方法实体
  var name = globalMy.foundName(this);
  var this_ = globalMy.jsdom_element[name];
  result = this_.webkitExitFullscreen.apply(this_, arguments);
  if (result == undefined || result == null) {} else {
    switch (typeof result) {
      case "object":
        debugger;
        break;
      case "function":
        debugger;
        break;
      default:
        break;
    }
  }
  if (globalMy.is_log) {
    globalMy.console.log('[*]  调用了HTMLVideoElement_webkitExitFullscreen ' + '  result => ', '' + result);
  }
  return result;
};